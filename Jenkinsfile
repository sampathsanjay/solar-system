pipeline {
    agent any
    stages {
        stage('Check Node and NPM Versions') {
            steps {
                script {
                    // Runs commands to check node and npm versions
                    sh 'node -v'
                    sh 'npm -v'
                }
            }
        }
    }
}

